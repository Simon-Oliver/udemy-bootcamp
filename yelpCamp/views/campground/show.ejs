<% include ../partials/header %>
<div class="container">
    <h1><%= campInfo.name%></h1>
    <img src="<%= campInfo.image%>" alt="">
    <p><%= campInfo.description %></p>

    <p>
    <a class="btn btn-success" href="/campgrounds/<%= campInfo._id %>/comments/new">Add New Comment</a>
    </p>

<% campInfo.comments.forEach(e =>{ %>
<p> <strong><%= e.author %></strong> - <%= e.text %> </p>
<% })%>

</div>
<% include ../partials/footer %>